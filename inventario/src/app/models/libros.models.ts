export class Libros {
    constructor(
        titulo: string,
        autor: string,
        apublicacion?: string,
        editorial?: string,
        categoria?: string,
        sede?: string,
        _id?: string
    ){}
}